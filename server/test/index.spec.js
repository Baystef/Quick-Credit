import { expect } from 'chai';
import server from '../index';

describe('test', () => {
  it('should return a string', () => {
    expect('This is Quick Credit').to.equal('This is Quick Credit');
  });
});
